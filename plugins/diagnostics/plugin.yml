name: diagnostics
version: 1.4.0
api: ">=1.0,<2.0"
entrypoint: plugin.py
enabled: true
depends: []
required_services:
  - api
  - audit
  - permissions
  - repository
  - monitoring
description: Core health, API state, plugin diagnostics and safe reload commands.
